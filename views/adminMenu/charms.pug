extends ../base

include .././mixins/charmCard

block content    
    main.main 
        a(href=`/createCharm`) Create New Charm
        .main__container 
            h3 Charms
            each charm in objects
                +charmCard(charm)

