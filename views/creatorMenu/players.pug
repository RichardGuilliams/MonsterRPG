extends ../base

include .././mixins/cards/playerCard

block content    
    main.main 
        a(href=`/createPlayer`) Create New Player
        .main__container 
            h3 Players
            each Player in data.objects[0]
                +playerCard(player)

