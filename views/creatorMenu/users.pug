extends ../base

include .././mixins/cards/userCard

block content    
    main.main 
        .main__container 
            h3 Users
            each user in data.objects[0]
                +userCard(user)

